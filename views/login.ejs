<%- include('partials/header') %>



<form class="w-full max-w-lg mx-auto border border-gray-400 rounded-md p-6 my-[10%]" method="POST" action="/admin/login/admin">
  <div class="text-center my-4">
    <p class="font-semibold text-2xl">Login Page</p>
  </div>
  <div class="w-full w-full px-3">
    <label class="block uppercase tracking-wide text-gray-700 text-xs font-bold mb-2" for="grid-last-name">
      Email
    </label>
    <input name="email" class="appearance-none block w-full bg-gray-200 text-gray-700 border border-gray-200 rounded py-3 px-4 leading-tight focus:outline-none focus:bg-white focus:border-gray-500" id="grid-last-name" type="text" placeholder="xyz@gmail.com">
  </div>
  <div class="flex flex-wrap mt-4">
    <div class="w-full px-3">
      <label class="block uppercase tracking-wide text-gray-700 text-xs font-bold mb-2" for="grid-password">
        Password
      </label>
      <input name="password" class="appearance-none block w-full bg-gray-200 text-gray-700 border border-gray-200 rounded py-3 px-4 mb-3 leading-tight focus:outline-none focus:bg-white focus:border-gray-500" id="grid-password" type="password" placeholder="******************">
    </div>
  </div>
  <div>
    <% if (errors && errors.length > 0) { %>
    <% errors.forEach(function(err) { %>
    <p style="color:red;"><%= err.msg %></p>
    <% }) %>
    <% } %>
  </div>
  <div class="w-full px-3">
    <button type="submit" class="bg-blue-600 w-full text-center cursor-pointer mt-4 text-white py-2 px-3 rounded-md">Submit</button>
  </div>
</form>


<%- include('partials/footer') %>